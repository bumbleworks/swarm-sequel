require 'spec_helper'

describe Swarm::Sequel do
  it 'has a version number' do
    expect(Swarm::Sequel::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
